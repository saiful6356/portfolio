import './index.scss';
import React from 'react';
import Sidebar from '../Sidebar';
const Layout = () => {
    return (
        <div className="layout" style={{color: 'white'}}>
        <Sidebar/>
        </div>
    );
    }
export default Layout;